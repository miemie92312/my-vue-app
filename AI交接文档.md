# Vue3 后台管理项目 AI 交接文档

## 1. 项目背景

这是一个 Vue3 + Vite + Pinia + Vue Router + Element Plus 的后台管理项目。

用户是前端近乎 0 基础的新手，目前正在跟着课程视频和配套文档学习实现这个项目。

当前学习重点已经进入：

- 登录页
- 动态菜单
- 动态路由
- Pinia 状态管理
- 刷新后恢复路由
- 退出登录清理状态
- 路由守卫

## 2. 用户的辅导要求

后续接手的 AI 必须遵守下面这些规则：

- 不要一次性给完整大功能代码。
- 必须按“微步骤”教学，一次只推进一个小点。
- 每一步都要明确：
  - 修改哪个文件
  - 替换哪一段
  - 最好提供上下文参照
- 先用大白话解释这一步在干嘛，再给代码。
- 如果用户发来报错，先用大白话解释报错原因，再给最小修复方案。
- 绝对不要为修一个小错误，让用户整文件重写。
- 每次回复最后都要问：
  - `这一步跑通了吗？`

## 3. 当前项目学习材料

用户提供了以下课程材料，已经读过并用于定位当前问题：

- [讲课文档.md](D:/FrontendStudy/讲课文档.md)
- [静态数据.md](D:/FrontendStudy/静态数据.md)
- [各个页面样式.md](D:/FrontendStudy/各个页面样式.md)

## 4. 课程顺序的真实依赖关系

视频标题顺序容易误导。实际正确实现顺序应理解为：

1. 登录页静态搭建
2. 登录成功后拿到 `menuList` 和 `token`
3. 把 `menuList` / `token` 存入 Pinia
4. 左侧菜单从 `store.state.menuList` 动态渲染
5. 根据 `menuList` 动态 `addRoute`
6. 删除原本写死的权限页子路由
7. 做 Pinia 持久化和刷新恢复
8. 做退出登录清理
9. 做路由守卫和 404

换句话说，视频里的“左侧菜单动态展示”并不是一个独立闭环，它依赖后面多节内容。

## 5. 已经明确定位过的问题

### 5.1 已确认的直接白屏原因

之前项目白屏的直接原因是：

- 文件：`src/stores/index.js`
- 问题：`return { ... }` 中暴露了一个未定义的 `clean`
- 报错：`ReferenceError: clean is not defined`

这个问题已经指导用户修复：

- 已删除 `return` 里的 `clean`

### 5.2 已确认的左侧菜单结构问题

`src/components/CommonAside.vue` 中模板结构有问题：

- `el-aside` 曾经被写成自闭合
- `width=width` 不是正确的 Vue 绑定写法
- 子菜单点击时曾写成 `handleMenu()`，没有传 `subItem`

已经给用户的下一步指导是：

- 把 `CommonAside.vue` 的模板外层改成正常的
  - `<el-aside :width="width"> ... </el-aside>`
- 子菜单点击改为：
  - `@click="handleMenu(subItem)"`

目前用户已反馈：

- 删除 `clean` 后，页面不再是原先那种直接白屏
- 但左侧菜单栏显示异常

说明项目已经从“启动即崩”推进到了“布局/逻辑仍不完整”的阶段。

## 6. 当前代码状态判断

当前项目处于“静态实现”和“动态菜单/动态路由实现”混搭状态，容易出错。

典型表现：

- `CommonAside` 已经尝试切到 `store.state.menuList`
- `stores/index.js` 已经开始加入 `menuList`、`token`、`routeList/routerList`、`addMenu`
- `Login.vue` 已经尝试调用登录接口并写入 `Pinia`
- 但很多步骤还没完整接上

这意味着后续辅导时，应该优先做“回到一个能运行的最小闭环”，不要继续叠加新功能。

## 7. 目前最推荐的教学推进方式

后续请按下面顺序带用户推进，不要跳步：

### 阶段 A：先恢复一个稳定可运行的静态主框架

目标：

- `/#/home` 可以正常显示
- 左侧菜单显示正常
- Main 布局正常
- CommonHeader / CommonAside / CommonTag 不报错

建议做法：

1. 先修 `CommonAside.vue` 模板结构
2. 再检查 `CommonAside.vue` 中 `store`、`list`、`noChildren`、`hasChildren` 的基础逻辑
3. 必要时先临时退回静态菜单 `list`

### 阶段 B：再做登录和 Pinia 存储闭环

目标：

- 登录按钮能触发
- `Login.vue` 能调用 mock 接口
- `menuList` 和 `token` 能写进 store

重点检查：

- `useAllDataStore()` 是否加括号
- `useRouter` 是否正确导入
- 登录按钮是否绑定点击事件

### 阶段 C：再切换成动态菜单

目标：

- `CommonAside` 成功从 `store.state.menuList` 渲染菜单
- 不再依赖组件内写死菜单

### 阶段 D：再接动态路由

目标：

- 登录后根据 `menuList` 动态添加路由
- 非权限菜单不能通过地址栏硬进

重点：

- `import.meta.glob('../views/**/*.vue')`
- `routeArr.push(item)` 与 `routeArr.push(...item.children)` 不要写错

### 阶段 E：最后做刷新恢复、退出清理、路由守卫

目标：

- 刷新后路由还在
- 退出后状态重置
- 未登录访问后台时跳转登录页

## 8. 用户当前最需要的帮助方式

不是“直接把项目全部改完”，而是：

- 带着用户一点点理解
- 每一步只改一个点
- 解释这一步为什么现在做
- 让用户自己刷新、验证、反馈

用户更需要的是“教学式协作”，不是“外包式交付”。

## 9. 接手时建议的开场方式

推荐接手 AI 这样继续：

1. 先确认用户上一步是否已经完成：
   - `CommonAside.vue` 模板结构是否已替换
2. 如果还没改，就继续只做这一小步
3. 如果已经改完，再继续检查：
   - 左侧菜单现在是“样式乱了”还是“控制台还报错”
4. 根据反馈，进入下一微步骤

## 10. 一句话总结

这个项目现在不是“完全不会做”，而是“教程链条被拆散后，用户在中间步骤把动态菜单、登录、Pinia、动态路由混到一起了”。

最优策略不是继续往后堆功能，而是先恢复静态主框架，再重新按：

- 登录
- Pinia
- 动态菜单
- 动态路由
- 刷新恢复
- 退出清理
- 路由守卫

这条顺序一点点推进。
